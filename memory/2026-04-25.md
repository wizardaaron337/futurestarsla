# Memory - 2026-04-25

## Summary
Major push on Future Stars LA website. Redesigned home page trip cards to compact/expandable layout, fixed PEX/POC display, added dates with years, did full site audit, and fixed all 3 critical issues found.

## Site Readiness Audit (15:17-15:55)
Identified 10 items across 3 priority levels:

### Critical (3 items) — All Fixed ✅
1. **Dark mode / fs-utils missing on 12 pages** — Added `<link href="fs-utils.css">`, `<script src="fs-utils.js">`, dark mode toggle (🌙/☀️), and user name display to: trips.html, team.html, trip-planner.html, tournament-scraper.html, sortly-upload.html, privacy.html, contact.html, jersey-gallery.html, trip-tracker.html, dashboard.html, departments.html, pj-planner.html, trip-plans.html
2. **Auth guard missing localStorage fallback on 2 pages** — `bracket.html` and `packing-list.html` only checked `sessionStorage`, breaking on build ID refreshes. Added `localStorage` fallback.
3. **Baseball jerseys had no photos** — Added CSS sport-based coloring for baseball cards (blue caps, green field). Generated 19 SVG placeholder images with team colors that show player names. Created `scripts/generate-jersey-placeholders.sh`.

### High Priority (items 4-6) — Not Started
4. Orphaned pages (bracket.html, packing-list.html) still accessible
5. Trip data CSV may have newer entries not in crew-data.js
6. Mobile QA not done

### Medium Priority (items 7-10) — Not Started
7. "Need Packing" stat logic might show wrong numbers
8. Error handling (localStorage try/catch only on home page)
9. No favicon or PWA manifest
10. Site logo downloaded as fs.jpeg but no favicon configured

## Key Decisions
- Baseball placeholder SVGs for now, real photos later
- Sport-based CSS coloring on jersey cards: baseball gets blue badge/caps, soccer gets green
- Deployed 5 builds today (1777142758 through 1777146909)

## Home Page Trip Cards Redesign
- Compact row layout with sport emoji, name, status badge
- "▼ Details" button expands to show dates, location, POC, crew, leave/return, PEX card
- `toggleTripDetails()` must be global scope (outside `renderHome()`)
- PEX card shown in amber text on detail dropdown
- POC = first crew member name from CSV (column 2)
- Dates now always include year (e.g. "Mar 8, 2026" or "Mar 28 - Mar 30, 2026")
- `formatDate()` updated: removed `year: 'numeric'` from range end (2026 shown once), range shows "Mar 28 - Mar 30, 2026", single-day shows "March 8, 2026"

## Site Technical Details
- **Repo**: wizardaaron337/futurestarsla (branches: main=live, staging=test)
- **Live URL**: https://futurestarsla.com
- **Deploy**: `./deploy-quick.sh "message"` — auto-branch-aware, injects build IDs, git push triggers Cloudflare Pages
- **Auth**: simple username/password (auth-config.js), 6 users, 5 roles
- **Data**: All static (inventory in HTML JS, crew trips from CSV parsed by crew-data.js, tournaments in tournament-data.js)
- **Bug test**: `bug-test.sh` runs hourly via cron; auto-fixes failures
- **Images**: `/images/jerseys/` — 18 real soccer photos, 19 baseball SVG placeholders
# Memory - 2026-04-25

## Summary
Major push on Future Stars LA website. Redesigned home page trip cards to compact/expandable layout, fixed PEX/POC display, added dates with years, did full site audit, and fixed all 3 critical issues found.

## Site Readiness Audit (15:17-15:55)
Identified 10 items across 3 priority levels:

### Critical (3 items) — All Fixed ✅
1. **Dark mode / fs-utils missing on 12 pages** — Added `<link href="fs-utils.css">`, `<script src="fs-utils.js">`, dark mode toggle (🌙/☀️), and user name display to: trips.html, team.html, trip-planner.html, tournament-scraper.html, sortly-upload.html, privacy.html, contact.html, jersey-gallery.html, trip-tracker.html, dashboard.html, departments.html, pj-planner.html, trip-plans.html
2. **Auth guard missing localStorage fallback on 2 pages** — `bracket.html` and `packing-list.html` only checked `sessionStorage`, breaking on build ID refreshes. Added `localStorage` fallback.
3. **Baseball jerseys had no photos** — Added CSS sport-based coloring for baseball cards (blue caps, green field). Generated 19 SVG placeholder images with team colors that show player names. Created `scripts/generate-jersey-placeholders.sh`.

### High Priority (items 4-6) — Not Started
4. Orphaned pages (bracket.html, packing-list.html) still accessible
5. Trip data CSV may have newer entries not in crew-data.js
6. Mobile QA not done

### Medium Priority (items 7-10) — Not Started
7. "Need Packing" stat logic might show wrong numbers
8. Error handling (localStorage try/catch only on home page)
9. No favicon or PWA manifest
10. Site logo downloaded as fs.jpeg but no favicon configured

## Key Decisions
- Baseball placeholder SVGs for now, real photos later
- Sport-based CSS coloring on jersey cards: baseball gets blue badge/caps, soccer gets green
- Deployed 5 builds today (1777142758 through 1777146909)

## Home Page Trip Cards Redesign
- Compact row layout with sport emoji, name, status badge
- "▼ Details" button expands to show dates, location, POC, crew, leave/return, PEX card
- `toggleTripDetails()` must be global scope (outside `renderHome()`)
- PEX card shown in amber text on detail dropdown
- POC = first crew member name from CSV (column 2)
- Dates now always include year (e.g. "Mar 8, 2026" or "Mar 28 - Mar 30, 2026")
- `formatDate()` updated: removed `year: 'numeric'` from range end (2026 shown once), range shows "Mar 28 - Mar 30, 2026", single-day shows "March 8, 2026"

## Site Technical Details
- **Repo**: wizardaaron337/futurestarsla (branches: main=live, staging=test)
- **Live URL**: https://futurestarsla.com
- **Deploy**: `./deploy-quick.sh "message"` — auto-branch-aware, injects build IDs, git push triggers Cloudflare Pages
- **Auth**: simple username/password (auth-config.js), 6 users, 5 roles
- **Data**: All static (inventory in HTML JS, crew trips from CSV parsed by crew-data.js, tournaments in tournament-data.js)
- **Bug test**: `bug-test.sh` runs hourly via cron; auto-fixes failures
- **Images**: `/images/jerseys/` — 18 real soccer photos, 19 baseball SVG placeholders

---

## Afternoon Polish Session (16:30-16:47)
Fixed remaining UX issues following the site audit:

### Pages Fixed
1. **sortly-upload.html** — Added auth guard (redirect to index.html if not logged in), dark mode toggle (🌙/☀️), fs-utils CSS/JS. Was previously unprotected and no theme toggle.
2. **index.html (home page)** — Added nav-dropdown.js (dropdown menu), dark mode toggle, fs-utils CSS/JS. Previously had no nav or theme controls. User reported it was missing on both sortly-upload and home.
3. **trips.html** — Added responsive card layout for mobile (<768px). Desktop keeps the full 17-column table. Mobile shows compact card view with sport icon, name, status badge, dates, location, crew, leave info, PEX card, rental return. No more horizontal scrolling on phones.
4. **404.html** — Created branded 404 page with FS logo, gold "404", back-to-home button, and quick links to Tournaments/Inventory/Trips/Pack Manager. Cloudflare Pages auto-serves `404.html` from root.
5. **contact.html** — Contact form now actually sends messages. Replaced localStorage-only handler with `mailto:` approach (opens email client with pre-filled subject/body). Still saves to localStorage as backup.

### Decision
- Contact form uses `mailto:` since it's a static site with no backend. Info@futurestarsla.com is the target. No third-party service needed (Formspree/Web3Forms require interactive registration).

### Still Left
- Dashboard, departments, pj-planner, trip-tracker, trip-plans pages could use dark mode / nav / fs-utils polish
- Full site favicon
- User is reviewing pages to confirm readiness for v2026.4.X release to main
# Memory - 2026-04-25

## Summary
Major push on Future Stars LA website. Redesigned home page trip cards to compact/expandable layout, fixed PEX/POC display, added dates with years, did full site audit, and fixed all 3 critical issues found.

## Site Readiness Audit (15:17-15:55)
Identified 10 items across 3 priority levels:

### Critical (3 items) — All Fixed ✅
1. **Dark mode / fs-utils missing on 12 pages** — Added `<link href="fs-utils.css">`, `<script src="fs-utils.js">`, dark mode toggle (🌙/☀️), and user name display to: trips.html, team.html, trip-planner.html, tournament-scraper.html, sortly-upload.html, privacy.html, contact.html, jersey-gallery.html, trip-tracker.html, dashboard.html, departments.html, pj-planner.html, trip-plans.html
2. **Auth guard missing localStorage fallback on 2 pages** — `bracket.html` and `packing-list.html` only checked `sessionStorage`, breaking on build ID refreshes. Added `localStorage` fallback.
3. **Baseball jerseys had no photos** — Added CSS sport-based coloring for baseball cards (blue caps, green field). Generated 19 SVG placeholder images with team colors that show player names. Created `scripts/generate-jersey-placeholders.sh`.

### High Priority (items 4-6) — Not Started
4. Orphaned pages (bracket.html, packing-list.html) still accessible
5. Trip data CSV may have newer entries not in crew-data.js
6. Mobile QA not done

### Medium Priority (items 7-10) — Not Started
7. "Need Packing" stat logic might show wrong numbers
8. Error handling (localStorage try/catch only on home page)
9. No favicon or PWA manifest
10. Site logo downloaded as fs.jpeg but no favicon configured

## Key Decisions
- Baseball placeholder SVGs for now, real photos later
- Sport-based CSS coloring on jersey cards: baseball gets blue badge/caps, soccer gets green
- Deployed 5 builds today (1777142758 through 1777146909)

## Home Page Trip Cards Redesign
- Compact row layout with sport emoji, name, status badge
- "▼ Details" button expands to show dates, location, POC, crew, leave/return, PEX card
- `toggleTripDetails()` must be global scope (outside `renderHome()`)
- PEX card shown in amber text on detail dropdown
- POC = first crew member name from CSV (column 2)
- Dates now always include year (e.g. "Mar 8, 2026" or "Mar 28 - Mar 30, 2026")
- `formatDate()` updated: removed `year: 'numeric'` from range end (2026 shown once), range shows "Mar 28 - Mar 30, 2026", single-day shows "March 8, 2026"

## Site Technical Details
- **Repo**: wizardaaron337/futurestarsla (branches: main=live, staging=test)
- **Live URL**: https://futurestarsla.com
- **Deploy**: `./deploy-quick.sh "message"` — auto-branch-aware, injects build IDs, git push triggers Cloudflare Pages
- **Auth**: simple username/password (auth-config.js), 6 users, 5 roles
- **Data**: All static (inventory in HTML JS, crew trips from CSV parsed by crew-data.js, tournaments in tournament-data.js)
- **Bug test**: `bug-test.sh` runs hourly via cron; auto-fixes failures
- **Images**: `/images/jerseys/` — 18 real soccer photos, 19 baseball SVG placeholders
# Memory - 2026-04-25

## Summary
Major push on Future Stars LA website. Redesigned home page trip cards to compact/expandable layout, fixed PEX/POC display, added dates with years, did full site audit, and fixed all 3 critical issues found.

## Site Readiness Audit (15:17-15:55)
Identified 10 items across 3 priority levels:

### Critical (3 items) — All Fixed ✅
1. **Dark mode / fs-utils missing on 12 pages** — Added `<link href="fs-utils.css">`, `<script src="fs-utils.js">`, dark mode toggle (🌙/☀️), and user name display to: trips.html, team.html, trip-planner.html, tournament-scraper.html, sortly-upload.html, privacy.html, contact.html, jersey-gallery.html, trip-tracker.html, dashboard.html, departments.html, pj-planner.html, trip-plans.html
2. **Auth guard missing localStorage fallback on 2 pages** — `bracket.html` and `packing-list.html` only checked `sessionStorage`, breaking on build ID refreshes. Added `localStorage` fallback.
3. **Baseball jerseys had no photos** — Added CSS sport-based coloring for baseball cards (blue caps, green field). Generated 19 SVG placeholder images with team colors that show player names. Created `scripts/generate-jersey-placeholders.sh`.

### High Priority (items 4-6) — Not Started
4. Orphaned pages (bracket.html, packing-list.html) still accessible
5. Trip data CSV may have newer entries not in crew-data.js
6. Mobile QA not done

### Medium Priority (items 7-10) — Not Started
7. "Need Packing" stat logic might show wrong numbers
8. Error handling (localStorage try/catch only on home page)
9. No favicon or PWA manifest
10. Site logo downloaded as fs.jpeg but no favicon configured

## Key Decisions
- Baseball placeholder SVGs for now, real photos later
- Sport-based CSS coloring on jersey cards: baseball gets blue badge/caps, soccer gets green
- Deployed 5 builds today (1777142758 through 1777146909)

## Home Page Trip Cards Redesign
- Compact row layout with sport emoji, name, status badge
- "▼ Details" button expands to show dates, location, POC, crew, leave/return, PEX card
- `toggleTripDetails()` must be global scope (outside `renderHome()`)
- PEX card shown in amber text on detail dropdown
- POC = first crew member name from CSV (column 2)
- Dates now always include year (e.g. "Mar 8, 2026" or "Mar 28 - Mar 30, 2026")
- `formatDate()` updated: removed `year: 'numeric'` from range end (2026 shown once), range shows "Mar 28 - Mar 30, 2026", single-day shows "March 8, 2026"

## Site Technical Details
- **Repo**: wizardaaron337/futurestarsla (branches: main=live, staging=test)
- **Live URL**: https://futurestarsla.com
- **Deploy**: `./deploy-quick.sh "message"` — auto-branch-aware, injects build IDs, git push triggers Cloudflare Pages
- **Auth**: simple username/password (auth-config.js), 6 users, 5 roles
- **Data**: All static (inventory in HTML JS, crew trips from CSV parsed by crew-data.js, tournaments in tournament-data.js)
- **Bug test**: `bug-test.sh` runs hourly via cron; auto-fixes failures
- **Images**: `/images/jerseys/` — 18 real soccer photos, 19 baseball SVG placeholders

---

## Afternoon Polish Session (16:30-16:47)
Fixed remaining UX issues following the site audit:

### Pages Fixed
1. **sortly-upload.html** — Added auth guard (redirect to index.html if not logged in), dark mode toggle (🌙/☀️), fs-utils CSS/JS. Was previously unprotected and no theme toggle.
2. **index.html (home page)** — Added nav-dropdown.js (dropdown menu), dark mode toggle, fs-utils CSS/JS. Previously had no nav or theme controls. User reported it was missing on both sortly-upload and home.
3. **trips.html** — Added responsive card layout for mobile (<768px). Desktop keeps the full 17-column table. Mobile shows compact card view with sport icon, name, status badge, dates, location, crew, leave info, PEX card, rental return. No more horizontal scrolling on phones.
4. **404.html** — Created branded 404 page with FS logo, gold "404", back-to-home button, and quick links to Tournaments/Inventory/Trips/Pack Manager. Cloudflare Pages auto-serves `404.html` from root.
5. **contact.html** — Contact form now actually sends messages. Replaced localStorage-only handler with `mailto:` approach (opens email client with pre-filled subject/body). Still saves to localStorage as backup.

### Decision
- Contact form uses `mailto:` since it's a static site with no backend. Info@futurestarsla.com is the target. No third-party service needed (Formspree/Web3Forms require interactive registration).

### Still Left
- Dashboard, departments, pj-planner, trip-tracker, trip-plans pages could use dark mode / nav / fs-utils polish
- Full site favicon
- User is reviewing pages to confirm readiness for v2026.4.X release to main

---

## Deep Polish & Bug Fix Session (16:47-17:11)

### Inventory Data Extraction
- **Problem**: `INVENTORY` object was defined inline in `inventory-v2.html`, so `dashboard.html` couldn't access it. Same issue with `pack-manager.html` having its own copy.
- **Fix**: Created shared `/website/inventory-data.js` — single source of truth for all 38 jersey items (19 baseball + 19 soccer) with sizes, totals, and image paths.
- **Pages updated**:
  - `inventory-v2.html` — removed inline INVENTORY, loads `inventory-data.js` instead
  - `dashboard.html` — loads `inventory-data.js` for real stat computation
  - `pack-manager.html` — loads `inventory-data.js` instead of inline copy
- All 3 pages now share the same data — no drift possibility.

### Logout Bug (8 pages fixed)
- **Problem**: Auth writes to both `sessionStorage` and `localStorage`, but `logout()` only cleared `sessionStorage`. Users clicking Logout would still be logged in after page reload.
- **Fixed on**: `dashboard.html`, `departments.html`, `pj-planner.html`, `tournament-scraper.html`, `tournaments.html`, `trips.html`, `trip-tracker.html`, `trip-plans.html`
- **Fix**: Added `localStorage.clear()` before `sessionStorage.clear()` in all logout functions.

### Dashboard Enhanced
- **Stat cards** now compute from real `INVENTORY` data instead of hardcoded "7.9K"
  - Total Units: sums all baseball + soccer totals
  - Baseball: sums all 19 baseball jersey totals
  - Soccer: sums all 19 soccer jersey totals
  - Active Trips: reads from `fs_trips_pack` localStorage (pack-manager data)
- **Low Stock Alerts**: Now scans real inventory for items below 10 units and shows them
- **Pack Manager link** added to Quick Access cards on dashboard
- Updated Inventory card description to mention jersey card layout

### departments.html Fix
- Tournament Operations card's "Tournaments" link pointed to `trips.html` instead of `tournaments.html` — fixed.

### bug-test.sh
- Updated to remove checks for deleted pages (`bracket.html`, `packing-list.html`, `jersey-gallery.html`)

### PJ Trip Maker Enhancement
- Added `crew-data.js` as dependency so PJ can see real assigned trips
- Added "My Assigned Trips" section at top of folder list showing PJ-named trips from CREW_TRIPS
- Added direct booking links section in trip detail view:
  - Hotels.com booking URL (searches by destination + dates)
  - Expedia hotel booking URL
  - Enterprise rental car booking URL
  - Kayak comparison search
- Added Trip Cost Estimator section in trip detail view with fields for hotel rate, car rental rate, # of nights, # of rental days
- Added hotelRate, carRate, hotelNights, carDays fields to trip modal for PJ to input actual costs
- Automatic cost calculation: Hotel = hotelRate × hotelNights, Car = carRate × carDays, Total = Hotel + Car + Misc budget

### JS Validation
- All 17 HTML pages pass JS syntax check
- bug-test.sh passes 34/34 checks
- Hourly cron job `fs-bug-test` runs in isolated session

## Still Remaining
- **PJ tool** enhancements in progress: booking links and cost calculator
- Favicon for the site
- Dashboard, departments, pj-planner, trip-tracker, trip-plans still could use more polish
- User reviewing for release to main (v2026.4.X)
# Memory - 2026-04-25

## Summary
Major push on Future Stars LA website. Redesigned home page trip cards to compact/expandable layout, fixed PEX/POC display, added dates with years, did full site audit, and fixed all 3 critical issues found.

## Site Readiness Audit (15:17-15:55)
Identified 10 items across 3 priority levels:

### Critical (3 items) — All Fixed ✅
1. **Dark mode / fs-utils missing on 12 pages** — Added `<link href="fs-utils.css">`, `<script src="fs-utils.js">`, dark mode toggle (🌙/☀️), and user name display to: trips.html, team.html, trip-planner.html, tournament-scraper.html, sortly-upload.html, privacy.html, contact.html, jersey-gallery.html, trip-tracker.html, dashboard.html, departments.html, pj-planner.html, trip-plans.html
2. **Auth guard missing localStorage fallback on 2 pages** — `bracket.html` and `packing-list.html` only checked `sessionStorage`, breaking on build ID refreshes. Added `localStorage` fallback.
3. **Baseball jerseys had no photos** — Added CSS sport-based coloring for baseball cards (blue caps, green field). Generated 19 SVG placeholder images with team colors that show player names. Created `scripts/generate-jersey-placeholders.sh`.

### High Priority (items 4-6) — Not Started
4. Orphaned pages (bracket.html, packing-list.html) still accessible
5. Trip data CSV may have newer entries not in crew-data.js
6. Mobile QA not done

### Medium Priority (items 7-10) — Not Started
7. "Need Packing" stat logic might show wrong numbers
8. Error handling (localStorage try/catch only on home page)
9. No favicon or PWA manifest
10. Site logo downloaded as fs.jpeg but no favicon configured

## Key Decisions
- Baseball placeholder SVGs for now, real photos later
- Sport-based CSS coloring on jersey cards: baseball gets blue badge/caps, soccer gets green
- Deployed 5 builds today (1777142758 through 1777146909)

## Home Page Trip Cards Redesign
- Compact row layout with sport emoji, name, status badge
- "▼ Details" button expands to show dates, location, POC, crew, leave/return, PEX card
- `toggleTripDetails()` must be global scope (outside `renderHome()`)
- PEX card shown in amber text on detail dropdown
- POC = first crew member name from CSV (column 2)
- Dates now always include year (e.g. "Mar 8, 2026" or "Mar 28 - Mar 30, 2026")
- `formatDate()` updated: removed `year: 'numeric'` from range end (2026 shown once), range shows "Mar 28 - Mar 30, 2026", single-day shows "March 8, 2026"

## Site Technical Details
- **Repo**: wizardaaron337/futurestarsla (branches: main=live, dev=development)
- **Live URL**: https://futurestarsla.com
- **Deploy**: `./deploy-quick.sh "message"` — auto-branch-aware, injects build IDs, git push triggers Cloudflare Pages
- **Auth**: simple username/password (auth-config.js), 6 users, 5 roles
- **Data**: All static (inventory in HTML JS, crew trips from CSV parsed by crew-data.js, tournaments in tournament-data.js)
- **Bug test**: `bug-test.sh` runs hourly via cron; auto-fixes failures
- **Images**: `/images/jerseys/` — 18 real soccer photos, 19 baseball SVG placeholders

## Afternoon Polish Session (16:30-16:47)
Fixed remaining UX issues following the site audit:

### Pages Fixed
1. **sortly-upload.html** — Added auth guard, dark mode toggle, fs-utils CSS/JS.
2. **index.html (home page)** — Added nav-dropdown.js, dark mode toggle, fs-utils CSS/JS.
3. **trips.html** — Added responsive card layout for mobile (<768px).
4. **404.html** — Created branded 404 page.
5. **contact.html** — Contact form uses `mailto:` approach.

## Deep Polish & Bug Fix Session (16:47-17:11)
- Created shared `inventory-data.js` — single source of truth for 38 jersey items
- Fixed logout bug on 8 pages (only cleared sessionStorage, not localStorage)
- Dashboard enhanced with real inventory stats
- Fixed departments.html Tournament link

## PJ Trip Maker & Role Permissions Session (17:11-17:22)

### pj-planner.html Enhancements
- **"My Assigned Trips" section** — Shows real CREW_TRIPS where PJ is named in crew, with sport emoji, dates, location, PEX card
- **One-click booking links** — Hotels.com, Booking.com, Kayak Cars, Enterprise, Google Flights, Google Maps — all pre-filled with trip destination
- **Trip cost estimator** — Hotel Rate ($/night), Nights, Rental Rate ($/day), Days fields in trip modal. Auto-calculates totals in detail view
- **Pre-trip checklist** — Add checkbox items, check them off, progress bar shown in detail view. Inline editing, add/remove support
- **Print summary button** — Opens clean print-friendly page with all trip info, cost breakdown, checklist
- Added crew-data.js as dependency
- Added booking links section between trip info and notes

### Role-Based Navigation Overhaul
**PJ (logistics) now has:** Home, Dashboard, Tournaments, Inventory, Trips, Trip Planner, Schedules, Pack Manager, PJ Trip Maker, Trip Tracker, Trip Plans, Privacy, Contact
**Caleb (tournament) now has:** Added PJ Trip Maker + Sortly Upload (excluded from revenue/financial). Full access: Home, Dashboard, Tournaments, Inventory, Trips, Trip Planner, Schedules, Pack Manager, PJ Trip Maker, Sortly Upload, Trip Tracker, Trip Plans, Privacy, Contact
**Marlon (inventory) now has:** Home, Dashboard, Inventory, Sortly Upload, Pack Manager, Team, Trip Tracker, Privacy, Contact
**Owners (JR, Lane) have:** Everything including Departments

### Nav Fallback Sync
- Updated nav-dropdown.js fallback arrays and NAV_ITEMS to match auth-config.js
- Both files now add `pj-planner`, `sortly-upload`, `trip-tracker`, `trip-plans` to appropriate roles
- Removed `jersey-gallery` from fallback references

### Deployed to dev
- Committed to dev branch as `0b13c80a` — "PJ trip maker enhancements + role-based nav overhaul"
- Pushed via git; Cloudflare Pages will auto-build
- Still on dev branch — not merged to main yet
# 2026-04-25

## Nav Redesign — Top Bar with Dropdowns
- Replaced floating hamburger menu with a top nav bar with dropdown menus
- 5 tab groups: Home, Operations, Planning, Tools, Info
- Each tab opens a dropdown with grouped tools, sorted by task not alphabetically
- Right side: user name, dark mode toggle (from fs-utils), logout button
- Active page highlighted in gold in dropdown
- Escape / click-outside closes all dropdowns
- Merged to dev, then main — **v2026.4.3 deployed and live**

## Supabase Migration Started
- Aaron created Supabase project (ID: `ughyvvjmoxqgvkuvrswq`)
- Created `supabase-config.js` and `supabase-setup.sql`
- Tables created in Supabase (SQL Editor): `inventory`, `crew_trips`, `tournaments`, `site_users`
- All data migrated:
  - 38 inventory items (baseball + soccer jerseys)
  - 221 crew trips
  - 139 tournaments (includes soccer + baseball)
  - 6 user profiles
- RLS policies set to allow SELECT for anon key (read-only for public API)
- Service role key used for bulk inserts
- `supabase-client.js` helper created for page-level fetching (not yet wired into pages)
- Config: URL = `https://ughyvvjmoxqgvkuvrswq.supabase.co`, anon key and service role key captured
- Next: wire up pages (inventory first) to fetch from Supabase instead of hardcoded JS files
- Key decision: data management moves to Supabase dashboard UI — no more editing JS files for inventory/trip/tournament changes
